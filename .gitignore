# QA harness run artifacts — regenerated on every run, not source.
qa/out-*.txt
qa/result-*.json
qa/shot-*.png
node_modules/
qa/audit-*.json
www/
# Native projects are generated by `npx cap add android|ios`; commit them once created.
